/*
 * Reggaeton El Real - Security Fase 5
 * TinyMCE 4.7.13 was removed. This tiny compatibility shim keeps the
 * legacy admin.php calls harmless while native <textarea> controls are used.
 */
(function (global) {
    "use strict";

    if (!global) {
        return;
    }

    global.tinymce = {
        init: function () {
            return [];
        },
        triggerSave: function () {
            return undefined;
        }
    };
})(typeof window !== "undefined" ? window : this);
